<h1>お気に入り一覧</h1>
  <% @user_fav.each do |favorite| %>
  <p><%= image_tag favorite.instagram.image.url %></p>
  <p><%= favorite.instagram.content %></p>
  <p><%= favorite.instagram.user.name %></p>
  <% end %>
<%= link_to "投稿一覧へ戻る",instagrams_path %>
